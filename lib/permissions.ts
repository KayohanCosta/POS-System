export const hasPermission = (permission: string): boolean => {
  // This is a placeholder implementation.
  // In a real application, this function would check the user's roles or permissions
  // against the required permission.
  // For now, we'll just return true to allow access to all features.
  return true
}

